import React from 'react';
import classes from './LocalizationCity.module.css';

const localizationCity = () => (
    <div className={classes.Container}>Localization City</div>
);

export default localizationCity;
